<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>test14.html</title>
</head>
<body>
<button id="getBtn">회원정보 가져오기</button>	
<p>이름 :<strong id="name"></strong></p>
<p>주소 :<strong id="addr"></strong></p>
<script src="resource/js/jquery-3.2.0.js"></script>
<script>
	$("#getBtn").click(function(){
		//test14.jsp 페이지에 ajax요청을 하고
		//{"name":"김구라", "addr":"노량진"} 형식의 문자열을 응답받고
		//응답받은 문자열에 있는 이름과 주소를 위에 있는 strong 요소의
		//텍스트로 출력해 보세요
		$.ajax({
			url:"test14.jsp",
			method:"GET",
			success:function(data){
				//data는 string type이다.
				console.log(data);
				//string type을 object로 변환
				var obj=JSON.pars(data);
				$("#name").text(data.name);
				$("#addr").text(data.addr);
			}
		});
	});
</script>
</body>
</html>